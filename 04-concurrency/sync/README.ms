# Sync

Contoh kode ini menunjukkan cara menggunakan `sync.WaitGroup` untuk menunggu goroutine selesai di Go.

## Cara Menjalankan

1. Navigasikan ke folder `sync`.
2. Jalankan perintah `go run main.go`.